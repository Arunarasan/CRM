import UnderMaintenance from "@/components/UnderMaintenance";

export default function PurchaseOrderProfile() {
  return (
    <UnderMaintenance
      moduleName="Purchasing (PO Details)"
      description="Purchase order profiles and goods receipt tracking are currently undergoing scheduled maintenance."
    />
  );
}
