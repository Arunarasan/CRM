import UnderMaintenance from "@/components/UnderMaintenance";

export default function InvoiceProfile() {
  return (
    <UnderMaintenance
      moduleName="Billing (Invoice Details)"
      description="Invoice profile management and payment logs are currently undergoing scheduled maintenance."
    />
  );
}
