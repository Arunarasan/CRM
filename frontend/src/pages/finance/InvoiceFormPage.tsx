import UnderMaintenance from "@/components/UnderMaintenance";

export default function InvoiceFormPage() {
  return (
    <UnderMaintenance
      moduleName="Billing & Finance (Invoice Form)"
      description="Finance invoice creation and stage payment scheduling are currently undergoing scheduled maintenance."
    />
  );
}
