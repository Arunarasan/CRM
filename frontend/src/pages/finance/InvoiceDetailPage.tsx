import UnderMaintenance from "@/components/UnderMaintenance";

export default function InvoiceDetailPage() {
  return (
    <UnderMaintenance
      moduleName="Billing & Finance (Invoice Details)"
      description="Finance invoice profile view and collection logs are currently undergoing scheduled maintenance."
    />
  );
}
