import UnderMaintenance from "@/components/UnderMaintenance";

export default function PurchaseOrderBuilder() {
  return (
    <UnderMaintenance
      moduleName="Purchasing (PO Builder)"
      description="Purchase order creation and price comparisons are currently undergoing scheduled maintenance."
    />
  );
}
