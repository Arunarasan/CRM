import UnderMaintenance from "@/components/UnderMaintenance";

export default function PurchaseLayout() {
  return (
    <UnderMaintenance
      moduleName="Purchasing"
      description="Purchase requests, supplier management, purchase orders and goods receipt notes are currently undergoing scheduled maintenance."
    />
  );
}
